{
    "name": "finance-ai-tracker",
    "version": "1.0.0",
    "description": "Intelligent finance tracker with AI-powered transaction input.",
    "scripts": {
      "install-all": "npm install && npm install --prefix backend && npm install --prefix frontend",
      "dev": "concurrently \"npm start --prefix backend\" \"npm start --prefix frontend\""
    },
    "devDependencies": {
      "concurrently": "^8.2.2"
    }
  }
  